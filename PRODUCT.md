# Product

## Register

product

## Users

Windows 11 工作站上的动画、渲染和技术美术用户。主要场景是在本地 NVMe/SSD 上快速检查数千张 PNG 序列或常用视频文件，连续观看运动、逐帧检查细节，并在不同位置快速擦帧。

## Product Purpose

提供一个原生、低延迟、中文序列帧播放器。软件应在首帧可用后立即进入观看状态，优先保障当前播放位置，再利用用户分配的 RAM 逐步缓存其他帧。除单来源检查外，软件还提供双画面对比，用同一个播放时钟逐帧比较左右素材。软件不创建图片缓存目录，不修改源素材，退出后所有图片缓存自动释放。

成功标准：PNG 默认 60 FPS、单视频默认原生 FPS 播放；方向键精确前后移动一帧；时间轴和画面内拖动均可擦帧；当前帧同步显示秒数；滚轮可围绕鼠标位置缩放，中键可平移并能一键复位；播放范围既可拖动也可直接输入；最近拖入的 PNG 序列可跨重启重新打开；用户可设置进程内存目标与解码比例；所有操作集中在底部且播放组固定居中；双画面对比使用最长素材的共享时间轴，左右画面成对提交，短素材越界后显示纯黑；可用居中遮罩预览并导出常用输出尺寸；HEVC MP4 继承遮罩、FPS 和范围，在 90 MB 上限内使用尽可能高的码率；缓存、缓冲、导出、内存和帧状态始终清晰可见。

## Comparison Mode

- “对比画面”将画布拆为左右两侧；右侧来源由拖放落点确定，成功加载后两侧统一回到第 1 帧。“退出对比”释放右侧来源并保留主来源。
- 主要工作流覆盖视频↔视频与序列↔视频，底层来源模型仍兼容其他左右组合。
- 对比总帧数取左右素材帧数的最大值。短素材越界后该侧显示纯黑，不停留在末帧；拖回其有效范围后恢复正常画面。循环以最长素材的共享末帧为边界。
- 播放、暂停、逐帧、时间轴、画布擦帧、播放范围、FPS、循环、解码比例、遮罩和总内存设置均为共享控制。左右有效帧必须在同一共享目标下成对原子提交。
- 滚轮缩放、中键平移和“重置画面”只操作当前选中的画布，左右视图状态互不覆盖。
- 默认 25 GB 始终是整个进程的总目标；进入对比模式后左右缓存按 50/50 分配，而不是每侧各占 25 GB。
- 对比模式下每侧固定使用 `1920×1920` 虚拟画布：来源按原始尺寸居中，超出画布时才等比缩小。所有遮罩与无遮罩使用同一份完整方画布适应窗口缩放，遮罩只把开口映射为同比例子矩形，不能为了填满 Pane 再放大；与开口同尺寸的已裁视频使用完整画面。所有预设都只平移完整虚拟画布，使左右最终可见素材的内侧边缘贴近中线，越出各自 Pane 的画布区域由 Clip 裁掉。
- MP4 导出始终只读取主来源；右侧对比素材不进入导出快照。

## Brand Personality

克制、可信、安静。界面接近 Codex 桌面工具的中性产品语言：清晰层级、柔和圆角、系统字体、少量状态色，视觉退到素材之后。

## Anti-references

- 不采用游戏工业风、霓虹光、金属面板、扫描线或装饰性 HUD。
- 不采用玻璃拟态、渐变文字、巨型指标卡或重复卡片网格。
- 不把播放器做成网页仪表盘，也不为标准交互发明陌生控件。
- 不使用会打断观看的自动弹窗、装饰动画或高饱和背景。

## Design Principles

1. 画面优先：播放器素材是视觉中心，所有控制只在需要时表达状态。
2. 时间一致：画面、当前帧、时间轴和播放时钟必须始终指向同一帧。
3. 预算透明：内存上限、解码比例、已缓存帧数和前方就绪量必须可解释。
4. 连续工作：加载、切换和重扫采用事务式提交，失败时保留当前可用序列。
5. 无隐性持久化：不生成图片缓存文件，不修改或污染素材目录。
6. 导出可追踪：不弹确认框，真实编码进度、完成路径和错误均在底栏内联显示。
7. 对比确定性：左右来源只由一个共享时钟推进；缺失帧明确显示纯黑，不复用旧帧或隐式改写时间轴。

## Accessibility & Inclusion

- 支持完整键盘播放操作，方向键每次只移动一帧。
- 正文和控件文字对比度至少达到 WCAG AA，关键数据目标达到 AAA。
- 状态不能只依赖颜色，必须同时提供中文文本或数字。
- 不使用闪烁、弹跳或装饰性连续动画。
- 兼容 Windows DPI 缩放，控件保持可点击尺寸。
