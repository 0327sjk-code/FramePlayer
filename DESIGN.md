# Design System

## Direction

场景句：用户在 Windows 工作站上长时间检查高饱和渲染序列，深色应用外壳需要像 Codex 一样安静、熟悉，并退到画面之后。

设计采用克制的深色中性产品界面。没有工业纹理、发光描边或装饰渐变。应用背景为近黑，工具栏和状态栏只通过轻微明度差分层；遮罩区域使用更深的不透明黑色。

## Color Strategy

Restrained。白色用于主信息，实体浅灰用于次要信息，绿色用于主要动作、播放状态与缓存；时间轴通过同一绿色体系的明度和粗细区分状态，亮绿色只表示 MP4 导出进度。

所有源色以稳定的 sRGB Token 定义，运行时使用等价 ImGui 浮点值：

```css
:root {
  --color-bg: #0a0a0a;
  --color-surface: #151515;
  --color-surface-raised: #1e1e1e;
  --color-surface-hover: #2a2a2a;
  --color-surface-active: #343434;
  --color-border: #2a2a2a;
  --color-ink: #ffffff;
  --color-muted: #c5c8c6;
  --color-primary: #2f7a55;
  --color-primary-hover: #3a825e;
  --color-primary-active: #286a49;
  --color-export-progress: #4ade80;
  --color-timeline-playback: #f3f8f5;
  --color-timeline-hot-cache: #86c9a4;
  --color-timeline-cold-cache: #4a916a;
  --color-accent: #5fbdea;
  --color-warning: #dfb043;
  --color-danger: #ea574a;
  --color-mask: #040404;
}
```

## Typography

- Windows 中文系统字体：`Microsoft YaHei UI`。
- 拉丁和数字沿用同一字体，避免播放器数据出现字体跳变。
- 正文、按钮与状态：16 px。
- 当前帧、范围首尾与总帧数：16 px，保持数字对齐。
- 不使用展示字体、全大写标签或过宽字距。

## Layout

```text
┌ 顶部来源栏：来源路径 / 总帧数                      ┐
├───────────────────────────────────────────────┤
│                                               │
│      可缩放、可中键平移、自适应比例播放画布     │
│                                               │
├───────────────────────────────────────────────┤
│ 时间轴 + 当前帧 / 总帧 / 当前秒数 + 缓存覆盖     │
├───────────────────────────────────────────────┤
│ 双手柄播放范围 + 可输入起始帧 / 结束帧          │
├───────────────────────────────────────────────┤
│ 打开 / 重扫 / 对比 / 重置  居中播放组  FPS / 循环 │
│ 内存 / 解码 / 遮罩          导出位置 / MP4 / 打开 │
│ 状态 / 缓存 / 性能 / 绿色编码进度               │
└───────────────────────────────────────────────┘
```

- 顶部来源栏固定为 `44 px`，只显示被动信息。底部控制区宽屏为 `192 px`；逻辑宽度低于 `1340 px` 时为 `272 px`，将播放、快捷操作、资源设置和导出拆为独立行。
- 播放画布占据剩余空间，图像按比例完整适配，不默认裁切。
- 控件圆角 8 px，面板圆角 10 px，主要按钮与输入区统一为 36 px 高。
- 不使用侧边栏和重复卡片；所有按钮和可编辑设置集中在底部控制区，播放组以整个窗口为基准居中。

## Components

### Primary button

“导出 MP4”使用主色填充。其他按钮保持中性，播放中的按钮用较深主色和白色文字表达选中状态。

### Timeline

整条时间轴可拖动。8 px 森林绿轨道表示后台缓存总量，4 px 薄荷绿内层表示当前播放位置前方连续就绪区；不绘制从首帧延伸到当前位置的播放进度线，当前帧只由薄荷绿外圈与淡薄荷白中心的圆形游标表达。悬停提示同步写明播放位置和两种缓存含义。拖动立即暂停并请求最新目标帧，不排队处理过期目标。

### Playback range

- 独立双手柄轨道和 1-based 数字输入共同设置包含首尾的起始帧与结束帧，默认覆盖完整来源；Enter 或失焦后提交并跳到对应端点。
- 两个手柄不可交叉，允许起始帧等于结束帧；帧坐标立即更新，不做惯性或位置缓动。
- 连续播放受范围约束；循环开启时结束帧回到起始帧，关闭循环时停在结束帧。
- 时间轴、方向键和画布擦帧仍可检查范围外画面；Home/End 与“首帧/末帧”统一跳到当前播放范围端点。
- 同目录重扫和解码比例切换保留自定义范围；默认完整范围会随新增帧扩展；新目录重置为完整范围。

### Viewport scrub

鼠标左键在画布内按住并横向拖动。累计移动 2 px 前进或后退 1 帧，即灵敏度系数 `0.5 frame/px`。开始拖动时暂停播放，松开后保持在目标帧。

### Comparison viewport

- “对比画面”在原画布位置建立左右等宽画布，按钮切换为“退出对比”。右侧初始为空，并引导用户把视频或 PNG 序列拖入右侧；拖放落点位于右画布时只加载右侧来源。
- 右侧来源成功提交后，左右都从第 1 帧开始。视频↔视频、序列↔视频是主要使用路径，底层仍允许其他来源组合。
- 对比时间轴总帧数取左右来源的最大值。共享帧号超出某一侧总帧数时，该侧只显示纯黑，不保留最后一帧、不拉伸时间；擦帧回到有效帧后立即恢复。
- 左右只有一个共享传输状态：播放、逐帧、时间轴、画布擦帧、播放范围、FPS、循环、解码比例、遮罩和总内存均同时生效。循环边界使用最长素材的共享结束帧。
- 同一共享目标帧的左右结果成对原子提交；任一有效目标帧尚未就绪时保持上一对已提交画面，避免出现左右不同步的中间状态。越界纯黑视为该侧已就绪。
- 点击、滚轮或中键操作会选中对应画布；选中画布使用克制的主色边界提示，不改变共享播放状态。
- 退出对比后保留主来源、主画面缓存、当前帧和左侧视图；右侧来源、纹理、缓存和视图状态全部释放。再次进入时右侧重新为空。

### Viewport navigation

- 鼠标滚轮以光标位置为锚点，在 `25%–1600%` 范围内缩放，未触及安全边界时光标下的图像坐标保持不动。
- 鼠标中键拖动只改变视图中心，不暂停播放、不 Seek、不修改缓存；图像保持可找回，不允许完全拖出视口。
- “重置画面”恢复当前选中画布的适应窗口 `100%` 和居中状态，不改变另一侧视图、当前帧、遮罩、播放状态或解码比例。
- 新来源成功提交时自动重置；同一来源重扫、改变解码比例、窗口尺寸或 DPI 时保留归一化视图中心。

### Memory and decode controls

- 进程内存目标默认 `25 GB`，支持 `4–48 GB` 直接输入和 `-／+` 逐 GB 调整。
- 内存目标始终是进程级总预算；对比模式下可用缓存严格按左右 50/50 分配，界面显示左右合计缓存，不能把目标解释为每侧预算。
- 解码比例提供 `25% / 50% / 75% / 100%`。
- 默认 `100%`，并明确标注“原始像素”；低于 `100%` 标注“缩放预览”。
- 比例改变后建立新会话代次，旧解码结果不得进入新缓存。对比模式采用左右事务提交：两侧都成功才显示新比例；任一侧失败则双方回滚到旧比例，回滚完成前保持暂停且不提交混合比例画面对。

### Viewport mask

- 默认“无遮罩”，可选 `1080×1080 / 1920×1080 / 1080×1920 / 864×1080`。
- 遮罩以 `1920×1920` 源画布为基准居中映射；解码比例改变时可见框比例不变。
- 遮罩只绘制在预览图像范围内，不进入播放解码、RAM 缓存或源文件；MP4 导出将同一开口转换为居中偶数像素裁切。
- 对比模式每侧先建立固定 `1920×1920` 虚拟画布，再把来源按原始 `sourceWidth/sourceHeight` 居中放入；小尺寸不放大，任一边超过 `1920` 时才等比缩小。无论选择哪种遮罩，适应窗口和独立 Zoom/Pan 的导航对象始终是完整方画布；遮罩开口只按相同 canvas scale 映射为子矩形，不能单独 Fit 到 Pane。开口与来源求交后生成 `targetInCanvas` 和源 UV，空余区域显式绘制纯黑；与开口同尺寸的已裁视频使用完整 UV，不发生二次裁切。所有预设都平移完整虚拟画布，让左侧最终可见素材右边缘、右侧最终可见素材左边缘贴中线，移到 Pane 外的区域由 Clip 裁掉。若完整画布显示边长为 `D`，五档开口依次为 `D×D / 0.5625D×0.5625D / D×0.5625D / 0.5625D×D / 0.45D×0.5625D`。此基础对齐发生在缩放和平移之前，“重置画面”恢复它。
- 切换文件夹、重扫或切换解码比例时保持当前遮罩；重启软件后恢复“无遮罩”。

### MP4 export

- “导出位置”与“导出 MP4”固定在播放控制附近；输出采用 HEVC Main `p5` 的 `hvc1` MP4，导出开始和结束均不弹窗。
- 正在准备、编码或自动降码率重试时，底部状态行切换为 8 px 亮绿色（`#4ade80`）确定进度条和真实帧数；该专用色不复用播放、缓存和范围的低饱和主色。
- 导出结束后进度条消失；成功显示绿色路径，失败显示红色行内原因。
- 导出中禁用导出位置、导出按钮和 FPS，普通播放、逐帧和擦帧仍可使用。
- 未配置位置时显示“PNG 同目录”；自定义位置超过可用宽度时省略，悬停显示完整路径。
- 对比模式下导出仍只使用左侧主来源及其导出快照；右侧来源、纯黑越界区和双画面布局不进入 MP4。

### Empty, loading and error states

- 空状态直接说明“拖入 PNG 序列文件夹或视频文件”，并指向下方的“打开文件夹／打开当前序列”；底部“打开当前序列”只读取最近一次成功拖入的 PNG 序列路径。
- 首帧加载使用静态骨架和文字进度，不在画面中央放持续旋转图标。
- 错误在底部状态行内显示，可展开详情；只有用户主动打开文件夹时使用系统模态选择器。

## Motion

- 控件 hover/active/focus 使用 140–180 ms 指数 ease-out 状态过渡。
- 按下时文字下移 1 px；时间轴 hover/按住时仅增大滑块和轨道厚度，命中区和帧位置不做缓动。
- 遵循 Windows `SPI_GETCLIENTAREAANIMATION`；系统关闭客户区动画时所有状态立即到位。
- 播放画面不做淡入淡出，以免影响逐帧判断。
- 时间轴、缓存和数值立即更新，不使用弹跳或惯性动画。

## Keyboard

- `Space`：播放／暂停。
- `Left`：后退一帧。
- `Right`：前进一帧。
- `Home`：第一帧。
- `End`：最后一帧。
- `L`：切换循环。
- 不提供跳十帧快捷键。
- 对比模式下以上快捷键只发送一次共享命令，同时驱动左右来源，不分别切换两次。
